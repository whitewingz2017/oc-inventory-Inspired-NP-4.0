import Controller from "./controller";
import RegisterDefault from "./register";

export const ResourceNUI = (/* unused pure expression or super */ null && (Controller));
export const RegisterNUI = (/* unused pure expression or super */ null && (RegisterDefault));
